###############################################################################
# net.solarnetwork.central.user.export.dao.mybatis Configuration Admin properties
###############################################################################

###############################################################################
# cache.node.flush <milliseconds>
# cache.user.flush <milliseconds>
# 
# An interval, in milliseconds, at which to flush MyBatis cached data.

cache.node.flush = 900000
cache.user.flush = 900000
